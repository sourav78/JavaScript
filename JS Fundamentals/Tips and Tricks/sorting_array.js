//sort an array in assending and decending manner

const nums = [3, 6, 1, 2, 9, 4]

//assending order
const assending = nums.sort( (a, z) => a-z)
console.log(assending);

//deceding order
const decending = nums.sort( (a, z) => z - a)
console.log(decending);

const arr  = Array.from({length: 5}, (_, index) => index)
console.log(arr);

//create array from 0 - 100

const newArr = Array.from({length: 101}, (item, index) => index)
console.log(newArr);


const newArr1 = Array.from({length: 1001}, (item, index) => index)
console.log(newArr1);